/* 3.5 */
$x.ajaxStart();

/* 3.4 */
$( ':first' );

/* 3.3 */
$x.click();

/* 3.2 */
$.holdReady();

/* 3.0 */
$x.bind();

/* 1.10 */
$x.context;

/* 1.9 */
$.support;

/* 1.8 */
$x.andSelf();

/* 1.7 */
$x.live( fn );

/* 1.3 */
// eslint-disable-next-line self/no-box-model
$.boxModel.style;

// eslint-disable-next-line self/no-browser
$.browser;
