/* 3.5 */
$x.ajaxStart();

/* 3.4 */
$( ':first' );

/* 3.3 */
// eslint-disable-next-line self/no-camel-case
$.camelCase();

// eslint-disable-next-line self/no-event-shorthand
$x.click();

// eslint-disable-next-line self/no-is-function
$.isFunction();

// eslint-disable-next-line self/no-is-numeric
$.isNumeric();

// eslint-disable-next-line self/no-is-window
$.isWindow();

// eslint-disable-next-line self/no-now
$.now();

// eslint-disable-next-line self/no-proxy
$.proxy();

// eslint-disable-next-line self/no-type
$.type();

/* 3.2 */
// eslint-disable-next-line self/no-hold-ready
$.holdReady();

/* 3.0 */
// eslint-disable-next-line self/no-bind
$x.bind();

/* 1.10 */
// eslint-disable-next-line self/no-context-prop
$x.context;

/* 1.9 */
// eslint-disable-next-line self/no-support
$.support;

/* 1.8 */
// eslint-disable-next-line self/no-and-self
$x.andSelf();

/* 1.7 */
// eslint-disable-next-line self/no-live
$x.live( fn );

/* 1.3 */
// eslint-disable-next-line self/no-browser
$.browser;
